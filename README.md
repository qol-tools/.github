# qol-tools

Organization-wide configuration and workflows.

## Workflows

### Auto-label qol-tray plugins

Automatically adds the `qol-tray-plugin` topic to any repo containing a `plugin.toml` file.

Runs every 6 hours, or manually via workflow dispatch.

